# TODO - controllers backend

- [x] Inspect APIs expected by frontend (based on available code) — no existing module endpoints found
- [x] Create missing Laravel controllers (minimal safe JSON)
- [ ] Create only required migrations/models for CRUD if endpoints need persistence (not required for current safe mock controllers)

- [x] Add API routes (keep Sanctum/auth untouched)
- [x] Run `php artisan route:list` to validate


